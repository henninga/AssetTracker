<UserControl x:Class="AssetTracker.Views.ProgramSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:local="clr-namespace:AssetTracker.UserControls" 
             xmlns:cm="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
             xmlns:Triggers="clr-namespace:AssetTracker.Triggers">

    <DockPanel Margin="10 0 0 0">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
            <TextBox x:Name="SearchText" Width="200" AcceptsReturn="False" />
            <Button x:Name="ExecuteSearch" Content="Search" IsDefault="True" />
        </StackPanel>
        <ContentControl x:Name="Results" DockPanel.Dock="Bottom" Margin="0 15 0 0" HorizontalAlignment="Left" />
    </DockPanel>
</UserControl>
