<UserControl x:Class="AssetTracker.Views.AllProgramsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:AssetTracker.UserControls">
    <StackPanel>
        <local:HyperlinkButton x:Name="NewProgram" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal" >
                <TextBlock Text="+" Style="{StaticResource plusSign}" VerticalAlignment="Center"/>
                <TextBlock Text=" Nytt program" VerticalAlignment="Center"/>
            </StackPanel>
        </local:HyperlinkButton>
        <ContentControl x:Name="Results" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
