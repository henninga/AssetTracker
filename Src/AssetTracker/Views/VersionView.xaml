<UserControl x:Class="AssetTracker.Views.VersionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:local="clr-namespace:AssetTracker.UserControls" >

    <StackPanel Orientation="Vertical">
        <local:HyperlinkButton x:Name="NewLicense" Style="{StaticResource newLicense}" />
        <ListBox x:Name="Licenses" Style="{StaticResource licenses}" />
    </StackPanel>

</UserControl>
